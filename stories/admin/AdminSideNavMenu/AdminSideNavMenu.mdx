import { Meta, Canvas, Primary, Controls, Story } from '@storybook/blocks';
import * as Stories from './AdminSideNavMenu.stories';

<Meta of={Stories} />

# AdminSideNavMenu

```tsx
import { AdminSideNavMenu } from 'venomous-ui';
```

## Props

<Controls />
<Canvas of={Stories.Default} sourceState="none" />

## Example

```tsx
import { useLocation } from 'react-router-dom';
import { AdminSideNavMenu, type AdminSideNavMenuProps } from 'venomous-ui';

const LogoElement: React.FC = () => (
  <img
    src="https://raw.githubusercontent.com/BlaxBerry333/venomous-ui/962fa42bc6d3fc7ae799c44206d8289ead2f2f5b/public/favicon.svg"
    width={40}
    height={40}
    alt="logo"
    draggable={false}
  />
);

function App() {
  const { pathname } = useLocation();

  const menuItems = useMemo<AdminSideNavMenuProps['menuItems']>(
    () => [
      {
        label: 'Analysis',
        icon: 'hugeicons:pie-chart-02',
        url: '/analysis',
        onClick: () => navigate('/analysis'),
      },
      {
        label: 'Draft',
        icon: 'hugeicons:node-edit',
        subItems: [
          {
            label: 'list',
            icon: 'hugeicons:dashboard-square-01',
            url: '/draft/list',
            onClick: () => navigate('/draft/list'),
          },
          {
            label: 'create',
            icon: 'hugeicons:dashboard-square-add',
            url: '/draft/create',
            onClick: () => navigate('/draft/create'),
          },
        ],
      },
      {
        label: 'Logic',
        icon: 'hugeicons:hierarchy-square-01',
        subItems: [
          {
            label: 'list',
            icon: 'hugeicons:dashboard-square-01',
            url: '/logic/list',
            onClick: () => navigate('/logic/list'),
          },
          {
            label: 'create',
            icon: 'hugeicons:dashboard-square-add',
            url: '/logic/create',
            onClick: () => navigate('/logic/create'),
          },
        ],
      },
    ],
    [navigate, pathname],
  );

  return (
    <AdminSideNavMenu
      menuHeaderHeight={50}
      menuWidth={200}
      menuItems={menuItems}
      currentPath={pathname}
      menuHeaderElement={
        <>
          <LogoElement />
          <Text text={'I Library'} isTitle />
        </>
      }
    />
  );
}
```
