import { Meta, Canvas, Primary, Controls, Story } from '@storybook/blocks';
import * as Stories from './AdminSettingsDrawer.stories';

<Meta of={Stories} />

# AdminSettingsDrawer

```tsx
import { AdminSettingsDrawer, AdminSettingsDrawerBlock } from 'venomous-ui';
```

## Props

<Controls />
<Canvas of={Stories.Default} sourceState="none" />

## Examples

```tsx
import { AdminSettingsDrawer, AdminSettingsDrawerBlock, Text } from 'venomous-ui';

function App() {
  return (
    <AdminSettingsDrawer
      width={300}
      title="Settings"
      labelOfThemeMode="ThemeMode"
      labelOfThemePalettes="ThemePalettes"
      onChangeThemeMode={() => alert('Theme Mode Changed')}
      onChangeThemePalette={() => alert('Theme Palette Changed')}
    >
      <p>Other Custom Content</p>
      <p>Other Custom Content</p>
      <p>Other Custom Content</p>
      <br />

      <Text text={'Alphabet'} isLabel />
      <AdminSettingsDrawerBlock
        items={['A', 'B', 'C']}
        renderItem={(item) => <div>{item}</div>}
        isItemSelected={(item) => item === alphabet}
        isItemDisabled={(item) => item === alphabet}
        onItemClick={async (item) => {
          setAlphabet(item);
          await new Promise((resolve) => setTimeout(resolve, 200));
          alert(item);
        }}
      />
    </AdminSettingsDrawer>
  );
}
```
