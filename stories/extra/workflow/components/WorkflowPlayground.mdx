import { Meta, Canvas, Primary, Controls, Story } from '@storybook/blocks';
import * as Stories from './WorkflowPlayground.stories';

<Meta of={Stories} />

# WorkflowPlayground

Must be wrapped in a `<WorkflowWrapper>` component, and also need to specify the size of the `height`、`width` or will get an warning and nothing will be displayed.

## Example

<Canvas of={Stories.Default} sourceState="none" />

## Props

<Controls />
