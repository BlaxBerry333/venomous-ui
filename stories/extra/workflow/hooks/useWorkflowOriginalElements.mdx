import { Meta } from '@storybook/blocks';

# useWorkflowOriginalElements

Must be used inside `<WorkflowWrapper>` component.

Call this hook can get the `nodes` and `edges` of the first rendering of the `<WorkflowPlayground>`

Can be used to handle the saving-history-record toggle function.
