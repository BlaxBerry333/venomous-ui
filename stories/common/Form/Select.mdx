import { Meta, Canvas, Primary, Controls, Story } from '@storybook/blocks';
import * as Stories from './Select.stories';

<Meta of={Stories} />

# SelectInput

```tsx
import { Select } from 'venomous-ui';
```

## Props

<Controls />

## Examples

#### Default

<Canvas
  of={Stories.Default}
  sourceState="shown"
  source={{
    code: [
      'import { Select, type OptionType } from "venomous-ui";',
      '',
      'function App() {',
      '  const [value, setValue] = React.useState<OptionType["value"]>();',
      '',
      '  const options: OptionType[] = [',
      '    { label: "Option 1", value: "option1" },',
      '    { label: "Option 2", value: "option2" },',
      '    { label: "Option 3", value: "option3" },',
      '    { label: "Option disabled", value: "option-disabled", isDisabled: true },',
      '  ];',
      '',
      '  return (',
      '    <Select',
      '      options={options}',
      '      value={value}',
      '      onChange={(option) => setValue(option?.value)}',
      '    />',
      '  );',
      '}',
    ].join('\n'),
  }}
/>

### CustomOption

<Canvas
  of={Stories.CustomOption}
  sourceState="shown"
  source={{
    code: [
      'import { Select, type OptionType } from "venomous-ui";',
      '',
      'function App() {',
      '  const [value, setValue] = React.useState<OptionType["value"]>();',
      '',
      '  const options: OptionType[] = [',
      '    { label: "Option 1", value: "option1" },',
      '    { label: "Option 2", value: "option2" },',
      '    { label: "Option 3", value: "option3" },',
      '    { label: "Option disabled", value: "option-disabled", isDisabled: true },',
      '  ];',
      '',
      '  return (',
      '    <Select',
      '      options={options}',
      '      value={value}',
      '      onChange={(option) => setValue(option?.value)}',
      '      renderOption={(option) => (',
      '        <Flex row p={0.5}>',
      '          <Icon icon="solar:asteroid-bold-duotone" />',
      '          {option.label}',
      '        </Flex>',
      '      )}',
      '    />',
      '  );',
      '}',
    ].join('\n'),
  }}
/>
