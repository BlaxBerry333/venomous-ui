import { Meta, Canvas, Primary, Controls, Story } from '@storybook/blocks';
import * as Stories from './Buttons.stories';

<Meta of={Stories} />

# Button

```tsx
import { Button } from 'venomous-ui';
```

## Props

<Controls />
<Canvas of={Stories.Default} />

## Examples

#### Text

<Canvas
  of={Stories.WithIcon}
  sourceState="shown"
  source={{
    code: [
      "import { Button, Flex } from 'venomous-ui';",
      '',
      'function App() {',
      '  return (',
      '    <Flex row gap={2}>',
      '      <Button text="Abc" />',
      '      <Button text="Abc" isGhost />',
      '      <Button text="Abc" isOutlined />',
      '    </Flex>',
      '  );',
      '}',
    ].join('\n'),
  }}
/>

#### WithIcon

<Canvas
  of={Stories.WithIcon}
  sourceState="shown"
  source={{
    code: [
      "import { Button, Flex } from 'venomous-ui';",
      '',
      'function App() {',
      '  return (',
      '    <Flex row gap={2}>',
      '      <Button text="Abc" icon="mdi:home" iconWidth={20} iconPosition="start" />',
      '      <Button icon="mdi:home" iconWidth={20} />',
      '      <Button text="Abc" icon="mdi:home" iconWidth={20} iconPosition="end" />',
      '    </Flex>',
      '  );',
      '}',
    ].join('\n'),
  }}
/>

#### Circle

<Canvas of={Stories.Circle} sourceState="shown" />
