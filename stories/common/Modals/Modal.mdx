import { Meta, Canvas, Primary, Controls, Story } from '@storybook/blocks';
import * as Stories from './Modal.stories';

<Meta of={Stories} />

# Modal

```tsx
import { Modal, useModal } from 'venomous-ui';
```

## Props

<Controls />

## Examples

#### Default

<Canvas
  of={Stories.Default}
  sourceState="shown"
  source={{
    code: [
      "import { Modal, useModal, Button } from 'venomous-ui';",
      '',
      'function App() {',
      '  const { isOpen, openModal, closeModal } = useModal();',
      '  return (',
      '    <>',
      '      <Button text="Open Modal" onClick={openModal} />',
      '      <Modal',
      '        isOpen={isOpen}',
      '        closeModal={closeModal}',
      '        isPrevented={false}',
      '      >',
      '        <div style={{ height: "200px", width: "300px", backgroundColor: "pink" }} />',
      '      </Modal>',
      '    </>',
      '  );',
      '}',
    ].join('\n'),
  }}
/>
